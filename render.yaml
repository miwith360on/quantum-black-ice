# DEPRECATED: Render deployment removed. This file is retained only for historical reference.
# Do not use. Prefer Railway (Procfile + railway.toml) for backend and Vercel for frontend.
services:
  - type: web
    name: quantum-black-ice
    env: python
    region: oregon
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "cd backend && gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT quick_start:app"
    envVars:
      - key: PORT
        value: 8080
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: RENDER
        value: true
      - key: MESOWEST_API_TOKEN
        sync: false
      - key: GOOGLE_MAPS_API_KEY
        sync: false
    healthCheckPath: /api/health
