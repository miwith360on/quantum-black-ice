# Railway Deployment Configuration
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && gunicorn -w 4 --bind 0.0.0.0:$PORT quick_start_no_ws:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
FLASK_ENV = "production"
